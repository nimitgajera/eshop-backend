const mongoose = require("mongoose")
const categorySchema = mondoogeSchema({
    name: {
        type: String,
        required : true,
    },
    icon:{
        type: String,
    },
    color:{
        type: String,
    },
});
